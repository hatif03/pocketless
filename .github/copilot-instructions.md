# Copilot — Pocketless

Follow `AGENTS.md`.

Wake name is **Pocketless**. Home is people and promises. We join Google Meet; we do not host the call.

Voice Agent API auth is `Authorization: Bearer`. All other AssemblyAI APIs use the raw key. Never put `ASSEMBLYAI_API_KEY` in client bundles.
